# Context Map

这个仓库由多个独立功能的用户脚本组成，每个子文件夹对应一个脚本。

| 子文件夹 | 描述 |
|---------|------|
| `apple-music-enhanced/` | — |
| `google-custom-time-range/` | — |
| `jump-to-deepwiki/` | — |
| `old-driver-legend-rb/` | — |
| `quark-web-cloud-drive-direct-link-extractor/` | 夸克网盘网页版直链提取器。从文件列表或分享页获取直链，并提供外部下载器（Gopeed/IDM/Motrix）所需的 UA 与 Cookie 配置。 |
| `send-to-memos/` | — |
| `steamdb-dlc-extractor/` | SteamDB DLC 清单生成器。从 SteamDB 页面提取 DLC 列表，按模板生成解锁工具（OpenSteamTool 等）所需的清单文件。 |

每个子文件夹有各自的 `CONTEXT.md`，描述该脚本的用途、功能、技术实现等。